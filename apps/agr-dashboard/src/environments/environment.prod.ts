export const environment = {
  production: true,
  // Injected at build time via CI: sed -i replacement or Angular fileReplacements
  clerkPublishableKey: 'pk_live_REPLACE_WITH_YOUR_CLERK_KEY',
  apiBase: '/v1',
};
