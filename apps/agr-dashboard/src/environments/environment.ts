export const environment = {
  production: false,
  // Set your Clerk Dev publishable key here for local development.
  // Get it from https://dashboard.clerk.com → your app → API Keys.
  // Never commit a real key — use a local .env or angular.json fileReplacements.
  clerkPublishableKey: 'pk_test_REPLACE_WITH_YOUR_CLERK_DEV_KEY',
  apiBase: '/v1',
};
